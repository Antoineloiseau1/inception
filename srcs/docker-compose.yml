version: '3.9'

services:
  nginx:
    container_name: nginx
    volumes:
      - "wordpress:/var/www/html"
    build:
      context: requirements/nginx
      dockerfile: Dockerfile
      args:
        DOMAIN_NAME: ${DOMAIN_NAME}
        CERTS_: ${CERTS_}
    ports:
     -  "443:443"
    env_file: .env
    restart: always
  
  mariadb:
    container_name: mariadb
    build: 
      context: requirements/mariadb
      dockerfile: Dockerfile
    volumes:
      - "database:/var/lib/mysql"
    env_file: .env
    networks:
      - inception
    restart: always

volumes:
  database:
    driver: local
    driver_opts:
      type: 'none'
      o: bind
      device: '/home/anloisea/data/database'
  wordpress:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: '/home/anloisea/data/wordpress'

networks:
  inception:
    name: inception
